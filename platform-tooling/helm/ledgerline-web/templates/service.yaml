apiVersion: v1
kind: Service
metadata:
  name: {{ include "ledgerline-web.fullname" . }}
  labels:
    {{- include "ledgerline-web.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "ledgerline-web.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ledgerline-web.selectorLabels" . | nindent 4 }}
