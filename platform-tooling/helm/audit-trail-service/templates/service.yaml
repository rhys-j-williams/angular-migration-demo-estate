apiVersion: v1
kind: Service
metadata:
  name: {{ include "audit-trail-service.fullname" . }}
  labels:
    {{- include "audit-trail-service.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "audit-trail-service.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "audit-trail-service.selectorLabels" . | nindent 4 }}
