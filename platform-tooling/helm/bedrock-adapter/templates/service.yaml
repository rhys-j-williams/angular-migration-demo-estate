apiVersion: v1
kind: Service
metadata:
  name: {{ include "bedrock-adapter.fullname" . }}
  labels:
    {{- include "bedrock-adapter.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "bedrock-adapter.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "bedrock-adapter.selectorLabels" . | nindent 4 }}
