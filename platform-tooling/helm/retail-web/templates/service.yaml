apiVersion: v1
kind: Service
metadata:
  name: {{ include "retail-web.fullname" . }}
  labels:
    {{- include "retail-web.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "retail-web.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "retail-web.selectorLabels" . | nindent 4 }}
