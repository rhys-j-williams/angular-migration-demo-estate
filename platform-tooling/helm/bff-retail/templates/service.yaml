apiVersion: v1
kind: Service
metadata:
  name: {{ include "bff-retail.fullname" . }}
  labels:
    {{- include "bff-retail.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "bff-retail.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "bff-retail.selectorLabels" . | nindent 4 }}
