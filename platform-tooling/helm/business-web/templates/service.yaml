apiVersion: v1
kind: Service
metadata:
  name: {{ include "business-web.fullname" . }}
  labels:
    {{- include "business-web.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "business-web.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "business-web.selectorLabels" . | nindent 4 }}
