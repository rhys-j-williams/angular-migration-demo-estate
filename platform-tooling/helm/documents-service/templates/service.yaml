apiVersion: v1
kind: Service
metadata:
  name: {{ include "documents-service.fullname" . }}
  labels:
    {{- include "documents-service.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "documents-service.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "documents-service.selectorLabels" . | nindent 4 }}
