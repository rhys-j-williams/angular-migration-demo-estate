apiVersion: v1
kind: Service
metadata:
  name: {{ include "statements-api.fullname" . }}
  labels:
    {{- include "statements-api.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "statements-api.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "statements-api.selectorLabels" . | nindent 4 }}
