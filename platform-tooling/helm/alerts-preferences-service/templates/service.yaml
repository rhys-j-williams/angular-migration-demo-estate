apiVersion: v1
kind: Service
metadata:
  name: {{ include "alerts-preferences-service.fullname" . }}
  labels:
    {{- include "alerts-preferences-service.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "alerts-preferences-service.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "alerts-preferences-service.selectorLabels" . | nindent 4 }}
