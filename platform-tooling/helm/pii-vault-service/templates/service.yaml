apiVersion: v1
kind: Service
metadata:
  name: {{ include "pii-vault-service.fullname" . }}
  labels:
    {{- include "pii-vault-service.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "pii-vault-service.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "pii-vault-service.selectorLabels" . | nindent 4 }}
