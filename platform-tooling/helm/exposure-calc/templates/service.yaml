apiVersion: v1
kind: Service
metadata:
  name: {{ include "exposure-calc.fullname" . }}
  labels:
    {{- include "exposure-calc.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "exposure-calc.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "exposure-calc.selectorLabels" . | nindent 4 }}
