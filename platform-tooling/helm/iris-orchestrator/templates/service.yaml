apiVersion: v1
kind: Service
metadata:
  name: {{ include "iris-orchestrator.fullname" . }}
  labels:
    {{- include "iris-orchestrator.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "iris-orchestrator.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "iris-orchestrator.selectorLabels" . | nindent 4 }}
