apiVersion: v1
kind: Service
metadata:
  name: {{ include "beacon-notifications.fullname" . }}
  labels:
    {{- include "beacon-notifications.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "beacon-notifications.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "beacon-notifications.selectorLabels" . | nindent 4 }}
