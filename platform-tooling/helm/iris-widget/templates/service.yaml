apiVersion: v1
kind: Service
metadata:
  name: {{ include "iris-widget.fullname" . }}
  labels:
    {{- include "iris-widget.labels" . | nindent 4 }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ include "iris-widget.fullname" . }}-tls
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "iris-widget.selectorLabels" . | nindent 4 }}
